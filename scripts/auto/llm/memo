local_llm_1:LLMが行動をすると、次の行動を決定するまでずーっと行動を続ける。
つまり、openduckが指示を受けたら、次の指示が来るまでずーっと真っすぐ進み続ける。
LLMの思考速度が遅いと、ぜんぜんだめ。

local_llm_2:1を改善。「何秒間動作するか」というのもLLMに考えさせるようにした。

local_llm_3:2を改善。断定はできないが、LLMは過去の行動や状態を保持していないよう（考える毎にNew Chatしてる？）な感じがするので、
過去の行動・状態をプロンプトに含むようにした。

